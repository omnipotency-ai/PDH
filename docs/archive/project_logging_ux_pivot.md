Logging friction is the #1 UX blocker. User avoids logging on bad days (diarrhea) which are exactly when data matters most. Current form-based logging feels like a chore.

**Why:** The best data model is useless without data. User has ADHD and needs minimal-friction input.

**How to apply:** Next conversation should focus on:

- Voice/conversational logging ("I ate toast with butter and beans") → AI parses into structured entries
- Portion size capture without tedious forms
- AI prompts/nudges when logging gaps are detected
- Rethinking the Track page input flow for speed over completeness
- Consider: can Claude itself be the logging interface via conversation?

Also captured in this conversation:

- Status model rethink: none → building → like/don't like/watch/avoid (2x2 of tolerated × taste preference)
- Secondary "baseline" flag for control foods (stable enough to pair with new foods for testing)
- Portion size affects outcomes — needs tracking at log time and potentially safe/watch thresholds at registry level
