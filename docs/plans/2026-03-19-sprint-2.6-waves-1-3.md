# Sprint 2.6 Waves 1-3: Transit Map Build

## Session Prompt

Pick up from where we left off. Check memory for current state.

Key context:

- Sprint 2.6 Wave 0 (Base UI migration) is COMPLETE — full migration from `radix-ui`+`vaul` to `@base-ui/react` ^1.3.0. ~60 files. WQ-080 through WQ-086 done.
- WQ-319 (Dr. Poo quality sign-off) is IN PROGRESS — user collecting real data over 1-2 days. Don't block on it.
- Branch: `feat/sprint-2.5+` — user will commit Wave 0 work and may create a new branch.
- 1273 unit tests passing, typecheck clean, build clean.
- `.env.local` has dev Clerk keys active, `TEST_OPENAI_API_KEY` set.

**YOUR TASK: Execute Sprint 2.6 Waves 1-3 (Transit Map Build).**

Plan: `docs/plans/2026-03-17-sprint-2.6-transit-map-ui.md`
Work queue: `docs/WORK-QUEUE.md` (Sprint 2.6 section, Wave 1-3 items)
WIP log: `docs/WIP.md`

Work items:

- **Wave 1 (WQ-207):** Audit existing transit map components — inventory mock vs live, what to keep/delete/refactor.
- **Wave 2 (WQ-208 through WQ-212, WQ-039, WQ-040):** Build production transit map — container, line track, station node, station inspector, wire into Patterns page.
- **Wave 3 (WQ-213, WQ-214, WQ-147, WQ-160):** Delete legacy code — transitData.ts mock data, legacy components, dead feature flag, developer notes.

## Execution Rules

**Use sub-agent driven development.** You are the orchestrator. Dispatch sub-agents for all implementation work. Main conversation is coordination only — don't burn context reading files that sub-agents should read.

**Aggressively manage context.** Don't read large files yourself. Give sub-agents clear, self-contained briefs with file paths, line numbers, and exact specifications. Each sub-agent should be able to complete its task independently.

**Update logs as you go:**

- Have each sub-agent update `docs/WIP.md` with their task status and summary when they finish.
- Update `docs/WORK-QUEUE.md` status column at the end of each wave (not after every individual task).
- Update memory (`MEMORY.md`) with any significant decisions or state changes.

**Wave execution order:**

1. Start with Wave 1 (audit) — this informs Wave 2 decisions. Run as a single Explore agent.
2. Wave 2 tasks can be parallelized where they have no dependencies:
   - WQ-208 (container) + WQ-209 (line track) + WQ-210 (station node) can start in parallel
   - WQ-211 (station inspector) depends on WQ-210 (needs station tap handler)
   - WQ-212 (wire into Patterns) depends on WQ-208 (needs container component)
3. Wave 3 (cleanup) must come after Wave 2 is verified.

**Quality gates between waves:**

- After Wave 2: `bun run typecheck && bun run test:unit && bun run build`
- After Wave 3: Same checks + verify no broken imports

**Pre-reading for sub-agents (include in their briefs):**

- `src/hooks/useTransitMapData.ts` — the data hook (what data is available)
- `src/components/patterns/transit-map/` — existing components
- `src/data/transitData.ts` — mock data being replaced
- `src/pages/Patterns.tsx` — where transit map renders
- `shared/foodEvidence.ts` — evidence types and status enums
- `docs/adrs/0002-food-registry-and-canonicalization.md` — registry hierarchy

**Design constraints (from CLAUDE.md):**

- Calm & scannable UX — user may be stressed/symptomatic
- Progressive disclosure — lead with most important signal
- Accessibility mandatory — aria labels, keyboard nav, loading/error states
- Design system discipline — use tokenized colors, check for existing components
- No hard-coding personalization
- Base UI components only (no Radix) — migration just completed
