{
  "plan": {
    "title": "Nutrition Card — Wave-Based Parallel Implementation",
    "version": "2.0",
    "created": "2026-04-03",
    "totalTasks": 30,
    "totalWaves": 6,
    "decisions_ref": "memory/project_nutrition_card_decisions.md",
    "annotations_ref": "docs/plans/Worktree spec/user-annotations/",
    "global_context": {
      "description": "This section exists for human readers only. Agents receive their protocol embedded in each task.",
      "testing_philosophy": "TDD with convex-test + Vitest for logic. Playwright E2E for UI flows. NO React component tests. NO mocks except vi.useFakeTimers() and external network calls.",
      "dev_servers": {
        "app": "localhost:3005 (TanStack Start dev server)",
        "convex": "Convex dev running alongside (npx convex dev)"
      },
      "branch": "adams-rib",
      "verification_commands": {
        "typecheck": "bun run typecheck",
        "build": "bun run build",
        "lint": "bun run lint:fix",
        "test": "bun run test",
        "format": "bun run format",
        "e2e": "bunx playwright test",
        "smoke_app": "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005",
        "smoke_convex": "npx convex dev --once 2>&1 | tail -5"
      }
    },
    "task_protocol_template": {
      "_comment": "This exact block is embedded in every task under 'task_protocol'. Agents see it inline — no memory dependency.",
      "steps": [
        "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
        "2. TDD: Write failing tests FIRST (Convex tests for logic, Vitest for utilities). Watch them fail. Then implement code to make them pass.",
        "3. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
        "4. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200. Check Convex dev has no schema errors.",
        "5. Check acceptance criteria — every single one must pass.",
        "6. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
        "7. Update docs/WIP.md with task completion status.",
        "8. Commit to adams-rib branch with descriptive message referencing the task ID (e.g., 'W1-02: Add nutrition goals to profile schema').",
        "9. For UI tasks: Browser-test via Claude-in-Chrome on localhost:3005. Verify the component renders, interacts correctly, and console has no errors."
      ],
      "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md. Do NOT push broken code. Do NOT skip failing tests.",
      "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel. Check the task's files_to_modify list before starting.",
      "testing_rules": {
        "convex_functions": "Write convex-test tests in convex/*.test.ts. Use convex-test with real database. No mocks except vi.useFakeTimers().",
        "utility_functions": "Write Vitest tests in shared/__tests__/ or src/**/__tests__/. Pure unit tests.",
        "ui_flows": "Write Playwright E2E tests in e2e/*.spec.ts. Test real user flows against real backend.",
        "forbidden": "NO React component tests. NO render(). NO React Testing Library. NO mock data unless absolutely unavoidable (justify with a comment)."
      }
    },
    "waves": [
      {
        "wave": 0,
        "name": "Research",
        "description": "Read-only analysis tasks that produce decision documents. No code changes. Uses agent-team validation: 3 agents independently research, then cross-validate findings before finalising.",
        "agent_team_protocol": {
          "description": "Wave 0 uses adversarial validation to prevent research errors propagating into code waves.",
          "steps": [
            "1. THREE agents independently read the same source files and produce their own mapping/analysis.",
            "2. Agents exchange their outputs and identify contradictions, gaps, or assumptions.",
            "3. Agents produce a COMBINED document that resolves contradictions with evidence (file paths, line numbers).",
            "4. The combined document includes a 'Assumptions Requiring Validation' section listing anything that could not be resolved from code alone.",
            "5. Human reviews the Assumptions section before Wave 1 begins."
          ],
          "gate": "Wave 1 CANNOT start until Wave 0 outputs are reviewed and the Assumptions section is resolved."
        },
        "task_protocol_research": {
          "steps": [
            "1. READ every file in files_to_read thoroughly. Do not skim.",
            "2. Produce the deliverable markdown document addressing every point in the description.",
            "3. Include exact file paths and line numbers for every claim.",
            "4. Include an 'Assumptions Requiring Validation' section at the end — list anything you inferred but could not confirm from code alone.",
            "5. Verify: cat the output file and confirm it meets the line count minimum.",
            "6. Commit to adams-rib branch with descriptive message."
          ],
          "rollback_strategy": "Research tasks produce documents only — no rollback needed. If uncertain about a finding, mark it as an assumption, do not guess."
        },
        "tasks": [
          {
            "id": "W0-01",
            "title": "Audit Agent A components vs real data model",
            "description": "Read all 9 Agent A files and the real domain types. Produce a line-by-line mapping document: for every type, interface, function, and import in Agent A's code, document what the real-app equivalent is (or note it doesn't exist). Key deliverable: a markdown file listing every mock dependency and its replacement.\n\nSpecific mappings needed:\n- mockData.FoodItem -> real FoodRegistryEntry (shared/foodRegistryData.ts) + ingredientProfiles.nutritionPer100g\n- mockData.FoodLogEntry -> real FoodLog (domain.ts) backed by logs table (type='food')\n- mockData.SavedMeal -> no real equivalent (defer or cut)\n- mockData.FOOD_REGISTRY -> shared/foodRegistry.ts FOOD_REGISTRY (148 entries, no macros/portions)\n- mockData.calculateCalories -> must compute from ingredientProfiles.nutritionPer100g.kcal * portion / 100\n- mockData.detectMealSlot -> corrected times: breakfast 5-9am, lunch 1-4pm, dinner 8-11pm, else snack\n- State.waterIntake/waterGoal -> derive from fluid logs (type='fluid', name='Water') in SyncedLogs\n- State.foodLogs -> derive from food logs (type='food') in SyncedLogs\n- State.favourites -> no existing storage; needs new field in profiles\n\nCRITICAL: Also audit the ingredientProfiles table to determine which entries already have nutritionPer100g data (kcal, protein, carbs, fat, sugars, fibre). Document coverage: how many of the 148 registry entries have matching ingredientProfiles with nutrition data? This directly feeds W1-01.",
            "files_to_read": [
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/mockData.ts",
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/useNutritionStore.ts",
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/NutritionCard.tsx",
              "src/types/domain.ts",
              "shared/foodRegistryData.ts",
              "convex/schema.ts",
              "convex/validators.ts",
              "src/contexts/SyncedLogsContext.tsx",
              "convex/ingredientProfiles.ts"
            ],
            "files_to_modify": [
              "docs/nutrition-card/data-model-mapping.md"
            ],
            "depends_on": [],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ every file in files_to_read thoroughly. Do not skim.",
                "2. Produce the deliverable markdown document addressing every point in the description.",
                "3. Include exact file paths and line numbers for every claim.",
                "4. Include an 'Assumptions Requiring Validation' section at the end — list anything you inferred but could not confirm from code alone.",
                "5. Verify: cat the output file and confirm it meets the line count minimum.",
                "6. Commit to adams-rib branch with descriptive message."
              ],
              "rollback_strategy": "Research tasks produce documents only — no rollback needed. If uncertain about a finding, mark it as an assumption, do not guess.",
              "agent_team": "This task runs 3x independently. Outputs are cross-validated before finalising."
            },
            "acceptance_criteria": [
              "Mapping doc covers every type, interface, and function in Agent A's 9 files",
              "Each mock dependency has a real-app equivalent documented (or explicitly marked as 'does not exist')",
              "Meal slot times use corrected values: breakfast 5-9am, lunch 1-4pm, dinner 8-11pm, else snack",
              "Doc identifies all fields that need to be created (nutrition goals, favourites, portion data)",
              "ingredientProfiles nutrition coverage audit: exact count of entries with/without nutritionPer100g data",
              "Includes 'Assumptions Requiring Validation' section"
            ],
            "verification": [
              "cat docs/nutrition-card/data-model-mapping.md | wc -l (should be >120 lines)"
            ],
            "outputs": [
              "docs/nutrition-card/data-model-mapping.md"
            ]
          },
          {
            "id": "W0-02",
            "title": "Map food pipeline integration points",
            "description": "Read the existing food parsing pipeline end-to-end and document exactly how the Nutrition card must call it.\n\nKey questions to answer:\n1. When user types 'chicken, 200g rice' and hits Log Food, what exact call sequence fires?\n2. How does the staging modal's 'Log Food' button work with the pipeline? Pre-populate items with matched canonicals, OR reconstruct rawInput string?\n3. How does FoodMatchingModal get triggered for unresolved items? Via useUnresolvedFoodQueue + useUnresolvedFoodToast.\n4. What is the mealSlot field format? Lowercase: 'breakfast', 'lunch', 'dinner', 'snack'\n\nDeliverable: markdown doc with exact function signatures, call sequences, and data shapes.",
            "files_to_read": [
              "src/hooks/useFoodParsing.ts",
              "src/hooks/useFoodLlmMatching.ts",
              "convex/foodParsing.ts",
              "convex/foodLlmMatching.ts",
              "shared/foodParsing.ts",
              "shared/foodMatching.ts",
              "src/components/track/FoodMatchingModal.tsx",
              "src/lib/syncLogs.ts"
            ],
            "files_to_modify": [
              "docs/nutrition-card/pipeline-integration.md"
            ],
            "depends_on": [],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ every file in files_to_read thoroughly. Do not skim.",
                "2. Produce the deliverable markdown document addressing every point in the description.",
                "3. Include exact file paths and line numbers for every claim.",
                "4. Include an 'Assumptions Requiring Validation' section at the end — list anything you inferred but could not confirm from code alone.",
                "5. Verify: cat the output file and confirm it meets the line count minimum.",
                "6. Commit to adams-rib branch with descriptive message."
              ],
              "rollback_strategy": "Research tasks produce documents only — no rollback needed. If uncertain about a finding, mark it as an assumption, do not guess.",
              "agent_team": "This task runs 3x independently. Outputs are cross-validated before finalising."
            },
            "acceptance_criteria": [
              "Doc answers all 4 key questions with exact function signatures",
              "Call sequence diagram from user input to database storage",
              "mealSlot format documented as lowercase strings",
              "Includes 'Assumptions Requiring Validation' section"
            ],
            "verification": [
              "cat docs/nutrition-card/pipeline-integration.md | wc -l (should be >80 lines)"
            ],
            "outputs": [
              "docs/nutrition-card/pipeline-integration.md"
            ]
          },
          {
            "id": "W0-03",
            "title": "Audit fluid migration scope",
            "description": "Document the exact fluid migration plan:\n1. Read FluidSection.tsx, useHabitLog.ts (handleLogFluid), fluidLogDataValidator\n2. Identify which fluid preset names map to food registry entries\n3. Document the Water special case: water stays as type='fluid', managed by WaterModal\n4. For non-water drinks: document how to log them as type='food' with rawInput\n5. Document which FluidSection elements get removed vs kept\n6. Document profile.fluidPresets migration\n\nDeliverable: markdown doc with migration table and decision rationale.",
            "files_to_read": [
              "src/components/track/panels/FluidSection.tsx",
              "src/hooks/useHabitLog.ts",
              "convex/validators.ts",
              "src/hooks/useProfile.ts",
              "shared/foodRegistryData.ts"
            ],
            "files_to_modify": [
              "docs/nutrition-card/fluid-migration.md"
            ],
            "depends_on": [],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ every file in files_to_read thoroughly. Do not skim.",
                "2. Produce the deliverable markdown document addressing every point in the description.",
                "3. Include exact file paths and line numbers for every claim.",
                "4. Include an 'Assumptions Requiring Validation' section at the end — list anything you inferred but could not confirm from code alone.",
                "5. Verify: cat the output file and confirm it meets the line count minimum.",
                "6. Commit to adams-rib branch with descriptive message."
              ],
              "rollback_strategy": "Research tasks produce documents only — no rollback needed. If uncertain about a finding, mark it as an assumption, do not guess.",
              "agent_team": "This task runs 3x independently. Outputs are cross-validated before finalising."
            },
            "acceptance_criteria": [
              "Migration table maps every fluid preset to its food registry equivalent",
              "Water special case documented (stays as type='fluid')",
              "Profile fluidPresets migration plan documented",
              "Includes 'Assumptions Requiring Validation' section"
            ],
            "verification": [
              "cat docs/nutrition-card/fluid-migration.md | wc -l (should be >50 lines)"
            ],
            "outputs": [
              "docs/nutrition-card/fluid-migration.md"
            ]
          },
          {
            "id": "W0-04",
            "title": "Design portion data schema for food registry",
            "description": "Design the portion data extension for FoodRegistryEntry.\n\nCRITICAL: The ingredientProfiles table in Convex ALREADY has nutritionPer100g data for many entries. This task MUST audit that table first and design a strategy to PULL existing data from ingredientProfiles rather than hallucinating values.\n\nRequired fields per entry:\n- defaultPortionG: number\n- naturalUnit: string | null ('slice', 'piece', 'tsp', 'cup', null)\n- unitWeightG: number | null (weight of one naturalUnit in grams)\n- caloriesPer100g, proteinPer100g, carbsPer100g, fatPer100g, sugarsPer100g, fibrePer100g: number\n\nApproach:\n1. Query ingredientProfiles table to see which entries already have nutritionPer100g\n2. For entries WITH data: map ingredientProfiles.nutritionPer100g fields to the new schema\n3. For entries WITHOUT data: document which ones are missing and create a population strategy (NOT agent-hallucinated values — use USDA FoodData Central or the existing ingredientProfiles API)\n4. Design a seeding script that pulls from ingredientProfiles at build time\n\nDeliverable: TypeScript interfaces + audit of existing data coverage + seeding strategy.",
            "files_to_read": [
              "shared/foodRegistryData.ts",
              "shared/foodRegistry.ts",
              "convex/ingredientProfiles.ts",
              "convex/schema.ts"
            ],
            "files_to_modify": [
              "docs/nutrition-card/portion-schema-design.md"
            ],
            "depends_on": [],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ every file in files_to_read thoroughly. Do not skim.",
                "2. Produce the deliverable markdown document addressing every point in the description.",
                "3. Include exact file paths and line numbers for every claim.",
                "4. Include an 'Assumptions Requiring Validation' section at the end — list anything you inferred but could not confirm from code alone.",
                "5. Verify: cat the output file and confirm it meets the line count minimum.",
                "6. Commit to adams-rib branch with descriptive message."
              ],
              "rollback_strategy": "Research tasks produce documents only — no rollback needed. If uncertain about a finding, mark it as an assumption, do not guess.",
              "agent_team": "This task runs 3x independently. Outputs are cross-validated before finalising."
            },
            "acceptance_criteria": [
              "TypeScript interfaces defined for portion data extension",
              "Audit of ingredientProfiles coverage: exact count with/without nutrition data",
              "Seeding strategy that pulls from ingredientProfiles — NOT hallucinated values",
              "Plan for populating gaps using USDA FoodData Central or equivalent",
              "Includes 'Assumptions Requiring Validation' section"
            ],
            "verification": [
              "cat docs/nutrition-card/portion-schema-design.md | wc -l (should be >100 lines)"
            ],
            "outputs": [
              "docs/nutrition-card/portion-schema-design.md"
            ]
          }
        ]
      },
      {
        "wave": 1,
        "name": "Foundation",
        "description": "Schema changes, registry extensions, and shared data model work. All schema changes must verify Convex dev picks them up successfully.",
        "tasks": [
          {
            "id": "W1-01",
            "title": "Add portion + macro data to food registry (seeded from ingredientProfiles)",
            "description": "Extend FoodRegistryEntryBase with optional portion/macro fields. Create FOOD_PORTION_DATA map. Populate using data from the existing ingredientProfiles Convex table — NOT by hallucinating values.\n\nNew fields (all optional): defaultPortionG, naturalUnit, unitWeightG, caloriesPer100g, proteinPer100g, carbsPer100g, fatPer100g, sugarsPer100g, fibrePer100g.\n\nApproach:\n1. Write a seeding script (scripts/seedPortionData.ts) that queries ingredientProfiles and maps nutritionPer100g to the new fields\n2. For entries without ingredientProfiles data, flag them — do NOT invent values\n3. Create FOOD_PORTION_DATA as a static map seeded from the script output\n4. Add utility functions to shared/foodRegistryUtils.ts:\n   - getPortionData(canonical)\n   - calculateCaloriesForPortion(canonical, portionG)\n   - calculateMacrosForPortion(canonical, portionG) -> {protein, carbs, fat, sugars, fibre}\n\nTDD: Write tests for utility functions FIRST. Tests should verify calculations against known values from ingredientProfiles.\n\nRun assertFoodRegistryInvariants + typecheck.",
            "files_to_read": [
              "docs/nutrition-card/portion-schema-design.md",
              "shared/foodRegistryData.ts",
              "shared/foodRegistryUtils.ts",
              "shared/foodRegistry.ts",
              "convex/ingredientProfiles.ts"
            ],
            "files_to_modify": [
              "shared/foodRegistryData.ts",
              "shared/foodRegistryUtils.ts",
              "shared/foodRegistry.ts",
              "scripts/seedPortionData.ts"
            ],
            "depends_on": [
              "W0-04"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. TDD: Write failing tests FIRST (Vitest for utility functions: getPortionData, calculateCaloriesForPortion, calculateMacrosForPortion). Watch them fail. Then implement code to make them pass.",
                "3. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "4. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200. Check Convex dev has no schema errors.",
                "5. Check acceptance criteria — every single one must pass.",
                "6. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "7. Update docs/WIP.md with task completion status.",
                "8. Commit to adams-rib branch with descriptive message referencing the task ID (e.g., 'W1-01: Add portion + macro data to food registry').",
                "9. For UI tasks: Browser-test via Claude-in-Chrome on localhost:3005. Verify the component renders, interacts correctly, and console has no errors."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md. Do NOT push broken code. Do NOT skip failing tests.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel. Check the task's files_to_modify list before starting.",
              "testing_rules": {
                "tdd_targets": "shared/__tests__/foodPortionData.test.ts — test getPortionData, calculateCaloriesForPortion, calculateMacrosForPortion against known ingredientProfiles values",
                "forbidden": "NO React component tests. NO mock data for nutrition values — use real ingredientProfiles data."
              }
            },
            "acceptance_criteria": [
              "Seeding script queries ingredientProfiles and maps to portion data",
              "Entries WITHOUT ingredientProfiles data are flagged, not filled with invented values",
              "Utility functions getPortionData, calculateCaloriesForPortion, calculateMacrosForPortion exist and are typed",
              "Tests for utility functions written FIRST and pass",
              "assertFoodRegistryInvariants passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "shared/foodRegistryData.ts (modified)",
              "shared/foodRegistryUtils.ts (modified)",
              "shared/foodRegistry.ts (modified)",
              "scripts/seedPortionData.ts (new)",
              "shared/__tests__/foodPortionData.test.ts (new)"
            ]
          },
          {
            "id": "W1-02",
            "title": "Add nutrition goals + favourites to profile schema (combined)",
            "description": "COMBINED TASK (originally W1-02 + W1-03) to avoid file conflicts — both touch convex/schema.ts, convex/validators.ts, src/hooks/useProfile.ts.\n\nPart A — Nutrition Goals:\nAdd nutritionGoals field to profiles table in Convex: { dailyCalorieGoal: number, dailyWaterGoalMl: number }. Add mutation to update. Add useNutritionGoals() hook with defaults (1800 kcal, 1000ml).\n\nPart B — Favourites:\nAdd foodFavourites: string[] to profiles table (array of canonical names). Add mutations: addFoodFavourite, removeFoodFavourite. Add useFoodFavourites() hook.\n\nCONVEX SCHEMA PUSH: After modifying convex/schema.ts, verify Convex dev picks up the schema change. Run 'npx convex dev --once' and check for errors. If schema push fails (e.g., breaking change on existing data), document the error and flag as BLOCKED.\n\nTDD: Write Convex function tests for mutations FIRST (convex-test). Write utility tests for hooks.",
            "files_to_read": [
              "convex/schema.ts",
              "convex/validators.ts",
              "src/hooks/useProfile.ts",
              "docs/nutrition-card/data-model-mapping.md"
            ],
            "files_to_modify": [
              "convex/validators.ts",
              "convex/schema.ts",
              "src/hooks/useProfile.ts"
            ],
            "depends_on": [
              "W0-01"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. TDD: Write failing Convex tests FIRST (convex/*.test.ts) for: updateNutritionGoals mutation, addFoodFavourite mutation, removeFoodFavourite mutation. Watch them fail. Then implement code to make them pass.",
                "3. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "4. CONVEX SCHEMA PUSH: After modifying schema.ts, run 'npx convex dev --once 2>&1 | tail -20' and verify no errors. If schema push fails, STOP and report.",
                "5. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "6. Check acceptance criteria — every single one must pass.",
                "7. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "8. Update docs/WIP.md with task completion status.",
                "9. Commit to adams-rib branch with descriptive message referencing the task ID."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md. Do NOT push broken code. Do NOT skip failing tests.",
              "file_conflict_rule": "This is the ONLY task in Wave 1 that touches schema.ts, validators.ts, and useProfile.ts. No parallel conflict.",
              "testing_rules": {
                "tdd_targets": "convex/__tests__/nutritionGoals.test.ts — test updateNutritionGoals, addFoodFavourite, removeFoodFavourite mutations with convex-test",
                "forbidden": "NO React component tests. NO mocks."
              }
            },
            "acceptance_criteria": [
              "nutritionGoals field added to profiles table in Convex schema",
              "foodFavourites field added to profiles table in Convex schema",
              "Convex schema push succeeds (npx convex dev --once shows no errors)",
              "Mutations for updating goals and add/remove favourites exist and are tested",
              "useNutritionGoals hook returns calorieGoal and waterGoalMl with defaults",
              "useFoodFavourites hook returns favourites array and toggleFavourite function",
              "Convex tests written FIRST and pass",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "npx convex dev --once 2>&1 | tail -20",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "convex/validators.ts (modified)",
              "convex/schema.ts (modified)",
              "src/hooks/useProfile.ts (modified)",
              "convex/__tests__/nutritionGoals.test.ts (new)"
            ]
          },
          {
            "id": "W1-03",
            "title": "Build useNutritionData hook (real data layer)",
            "description": "Create src/hooks/useNutritionData.ts that reads from SyncedLogs and Convex.\n\nReturns: todayFoodLogs, todayFluidLogs, totalCaloriesToday, totalMacrosToday (5 values), waterIntakeToday, caloriesByMealSlot, logsByMealSlot, recentFoods.\n\nMealSlot detection: breakfast 5-9am, lunch 1-4pm, dinner 8-11pm, else snack.\n\nThis hook does NOT manage UI state — that stays in useNutritionStore.\n\nTDD: Write tests for the data transformation logic FIRST. Extract pure functions (getMealSlot, calculateTotalCalories, groupByMealSlot) and test them independently before wiring into the hook.",
            "files_to_read": [
              "src/contexts/SyncedLogsContext.tsx",
              "src/types/domain.ts",
              "docs/nutrition-card/data-model-mapping.md",
              "shared/foodRegistryUtils.ts"
            ],
            "files_to_modify": [
              "src/hooks/useNutritionData.ts"
            ],
            "depends_on": [
              "W0-01",
              "W0-02",
              "W1-01",
              "W1-02"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. TDD: Extract pure functions (getMealSlot, calculateTotalCalories, calculateTotalMacros, groupByMealSlot) into a testable module. Write failing Vitest tests for these functions FIRST. Watch them fail. Then implement.",
                "3. Wire the pure functions into the useNutritionData hook.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "6. Check acceptance criteria — every single one must pass.",
                "7. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "8. Update docs/WIP.md with task completion status.",
                "9. Commit to adams-rib branch with descriptive message referencing the task ID."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md. Do NOT push broken code. Do NOT skip failing tests.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel. Check the task's files_to_modify list before starting.",
              "testing_rules": {
                "tdd_targets": "src/hooks/__tests__/useNutritionData.test.ts — test getMealSlot (boundary times), calculateTotalCalories, calculateTotalMacros, groupByMealSlot",
                "forbidden": "NO React component tests. NO mocks except vi.useFakeTimers() for meal slot time boundaries."
              }
            },
            "acceptance_criteria": [
              "Hook returns todayFoodLogs, totalCaloriesToday, totalMacrosToday (5 values), waterIntakeToday, caloriesByMealSlot, logsByMealSlot, recentFoods",
              "Meal slot detection uses corrected times (breakfast 5-9am, lunch 1-4pm, dinner 8-11pm, else snack)",
              "Pure functions extracted and tested independently",
              "Tests written FIRST and pass",
              "Hook does NOT manage UI state",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/hooks/useNutritionData.ts (new)",
              "src/hooks/__tests__/useNutritionData.test.ts (new)"
            ]
          }
        ]
      },
      {
        "wave": 2,
        "name": "Core UI",
        "description": "Port Agent A's modular components into main repo, replacing mock data with real hooks and applying locked visual decisions. Every UI component gets Playwright E2E tests in this wave — NOT deferred to Wave 5.",
        "tasks": [
          {
            "id": "W2-01",
            "title": "Build useNutritionStore (UI state only)",
            "description": "Create useReducer-based hook for UI state: view, searchQuery, stagingItems, modals, mealSlot. Search uses Fuse.js against real FOOD_REGISTRY (threshold 0.4, min 3 chars). Staging aggregates same canonicalName. Staging persists across view changes.\n\nTDD: Write Vitest tests for the reducer FIRST — test every action type, staging aggregation logic, and search threshold behaviour. The reducer is a pure function, perfect for TDD.",
            "files_to_read": [
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/useNutritionStore.ts",
              "docs/nutrition-card/data-model-mapping.md",
              "shared/foodRegistry.ts"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/useNutritionStore.ts"
            ],
            "depends_on": [
              "W0-01",
              "W1-01"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. TDD: Write failing Vitest tests FIRST for the reducer: every action type, staging aggregation (same canonicalName collapses), search filtering (threshold 0.4, min 3 chars), staging persistence across view changes. Watch them fail. Then implement.",
                "3. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "4. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "5. Check acceptance criteria — every single one must pass.",
                "6. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "7. Update docs/WIP.md with task completion status.",
                "8. Commit to adams-rib branch with descriptive message referencing the task ID."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md. Do NOT push broken code. Do NOT skip failing tests.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel. Check the task's files_to_modify list before starting.",
              "testing_rules": {
                "tdd_targets": "src/components/track/nutrition/__tests__/useNutritionStore.test.ts — test all 14+ action types, staging aggregation, search thresholds, view persistence",
                "forbidden": "NO React component tests. NO mock data imports."
              }
            },
            "acceptance_criteria": [
              "useReducer with all 14+ action types",
              "Fuse.js search against real FOOD_REGISTRY (threshold 0.4, min 3 chars)",
              "Staging aggregates same canonicalName into one row",
              "Staging persists across view changes",
              "No mock data imports",
              "Tests for reducer written FIRST and pass",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/useNutritionStore.ts (new)",
              "src/components/track/nutrition/__tests__/useNutritionStore.test.ts (new)"
            ]
          },
          {
            "id": "W2-02",
            "title": "Build NutritionCard shell (collapsed + header + input)",
            "description": "Port from Agent A with visual decisions applied: D's header (icon + 'Nutrition'), orange icons, cyan water, D's calorie ring, A's search input, D's orange Log Food button, B's water progress bar. Global escape handler from D. Accessibility from D. Wires to both useNutritionStore and useNutritionData.\n\nPlaywright E2E: Write E2E test that navigates to Track page and verifies: NutritionCard renders, calorie ring visible, search input focusable, Log Food button present.",
            "files_to_read": [
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/NutritionCard.tsx",
              "docs/nutrition-card/data-model-mapping.md",
              "src/index.css"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/NutritionCard.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-01",
              "W1-03"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Build the NutritionCard component (no TDD for React components — tested via E2E).",
                "3. Write Playwright E2E test: navigate to Track page, verify NutritionCard renders with calorie ring, search input, Log Food button.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005. Verify the component renders, interacts correctly, and console has no errors."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md. Do NOT push broken code. Do NOT skip failing tests.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — verify NutritionCard renders on Track page with all key elements",
                "forbidden": "NO React component tests. NO render(). NO React Testing Library."
              }
            },
            "acceptance_criteria": [
              "Component renders collapsed card with D's header, orange icons, cyan water, calorie ring, search input, Log Food button, water progress bar",
              "Global escape handler closes expanded views",
              "Wires to useNutritionStore and useNutritionData",
              "No mock data imports",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005",
              "Browser test: component renders on localhost:3005 with no console errors"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/NutritionCard.tsx (new)",
              "e2e/nutrition-card.spec.ts (new or extended)"
            ]
          },
          {
            "id": "W2-03",
            "title": "Build StagingModal with match status indicators",
            "description": "Port from Agent A. Apply: A's centered position, A's row layout, C's 5-macro totals (protein/carbs/fat/sugars/fibre), C's buttons in D's orange, green tick/orange alert for match status. Natural units from V4 (slice/piece/tsp alongside grams). Accessibility: role=dialog, aria-modal, focus trap, escape to close.\n\nPlaywright E2E: Extend e2e/nutrition-card.spec.ts — search for a food, add to staging, verify staging modal opens with correct items, verify macro totals display.",
            "files_to_read": [
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/StagingModal.tsx",
              "docs/nutrition-card/data-model-mapping.md"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/StagingModal.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-01"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Build the StagingModal component.",
                "3. Write Playwright E2E test: search food → stage → verify modal, items, macros, match status indicators.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — staging flow: search → add → verify modal content + macros + match status",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Centered modal with A's row layout",
              "5-macro totals (protein/carbs/fat/sugars/fibre)",
              "Green tick for matched items, orange alert for unresolved",
              "Natural units displayed alongside grams",
              "role=dialog, aria-modal=true, focus trap, escape to close",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/StagingModal.tsx (new)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W2-04",
            "title": "Build WaterModal with C/D's composite design",
            "description": "Combine: A's centered position + prefill animation, B's icon in button, C's ring + plus/minus + buttons (cyan), D's heading + remaining text + escape. Reads waterIntake from useNutritionData, waterGoal from useNutritionGoals. Cyan: #38bdf8 (sky-400).\n\nPlaywright E2E: Extend e2e/nutrition-card.spec.ts — click water icon, verify modal opens, verify +/- controls work, verify ring updates.",
            "files_to_read": [
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/WaterModal.tsx",
              ".claude/worktrees/agent-a0b4b876/src/components/track/nutrition/WaterModal.tsx",
              ".claude/worktrees/agent-a73daffd/src/components/track/nutrition/WaterModal.tsx",
              "src/hooks/useHabitLog.ts"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/WaterModal.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-01"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Build the WaterModal component.",
                "3. Write Playwright E2E test: click water icon → modal opens → +/- controls → ring updates → escape closes.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — water modal flow: open → +/- → ring → close",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Centered modal with cyan ring (#38bdf8)",
              "Prefill animation, +/- controls, Log Water button with droplet icon",
              "Shows remaining ml below ring",
              "Escape to close",
              "Reads from useNutritionData + useNutritionGoals",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/WaterModal.tsx (new)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W2-05",
            "title": "Build CalorieDetailView with B's accordion",
            "description": "Combine: C's segmented color bar, C's per-slot calories, 5-column macros, B's one-open-at-a-time accordion. Corrected meal times: breakfast 5-9am, lunch 1-4pm, dinner 8-11pm, else snack. Delete button on food rows via removeSyncedLog.\n\nPlaywright E2E: Extend e2e/nutrition-card.spec.ts — tap calorie ring → detail view opens → accordion expands → food items visible → delete button works.",
            "files_to_read": [
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/CalorieDetailView.tsx",
              ".claude/worktrees/agent-aa467ec9/src/components/nutrition/CalorieDetailView.tsx"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/CalorieDetailView.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-01"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Build the CalorieDetailView component.",
                "3. Write Playwright E2E test: tap calorie ring → detail view → accordion → food items → delete.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — calorie detail: open → accordion → items → delete",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Segmented color bar by meal slot",
              "Per-slot calorie display",
              "5-column macros row",
              "One-open-at-a-time accordion",
              "Corrected meal times (breakfast 5-9am, lunch 1-4pm, dinner 8-11pm, else snack)",
              "Delete button on food rows",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/CalorieDetailView.tsx (new)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W2-06",
            "title": "Build FavouritesView + filter tabs + barrel export",
            "description": "FavouritesView: A's design, wired to useFoodFavourites. Filter view: Recent|Frequent|Favourites|All tabs (NOT meal slot tabs). Zero-state = recently logged foods for current meal slot. Create index.ts barrel export.\n\nPlaywright E2E: Extend e2e/nutrition-card.spec.ts — navigate to favourites, verify tabs work, verify zero-state shows recent foods.",
            "files_to_read": [
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/FavouritesView.tsx",
              ".claude/worktrees/agent-a31ddf8f/src/components/track/nutrition/MealSlotFilterView.tsx",
              "docs/plans/Worktree spec/research-food-filter-patterns.md"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/FavouritesView.tsx",
              "src/components/track/nutrition/MealSlotFilterView.tsx",
              "src/components/track/nutrition/index.ts",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-01",
              "W1-02"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Build the FavouritesView and MealSlotFilterView components + barrel export.",
                "3. Write Playwright E2E test: navigate to favourites → tabs → zero-state → add favourite.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — favourites: tabs → zero-state → add/remove favourite",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "FavouritesView: A's design, wired to useFoodFavourites",
              "Filter view: Recent|Frequent|Favourites|All tabs (NOT meal slot tabs)",
              "Zero-state shows recently logged foods for current meal slot",
              "Barrel export in index.ts",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/FavouritesView.tsx (new)",
              "src/components/track/nutrition/MealSlotFilterView.tsx (new)",
              "src/components/track/nutrition/index.ts (new)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          }
        ]
      },
      {
        "wave": 3,
        "name": "Integration",
        "description": "Wire UI to real food pipeline. Replace mock data paths with Convex calls. Mount in Track page. Includes error boundary and end-to-end integration test.",
        "tasks": [
          {
            "id": "W3-01",
            "title": "Wire Log Food button to food parsing pipeline",
            "description": "When user has staging items and hits Log Food: reconstruct rawInput from items ('200g chicken breast, 150g white rice'), call handleLogFood from useFoodParsing. For items already matched (matchStatus='matched'), optionally pre-fill items array to skip redundant matching. Clear staging on success. Food logging MUST go through addSyncedLog with type='food'.\n\nTDD: Write Convex test for the food logging flow. Playwright E2E: type food → stage → log → verify appears in TodayLog.",
            "files_to_read": [
              "src/hooks/useFoodParsing.ts",
              "src/lib/syncLogs.ts",
              "convex/foodParsing.ts",
              "docs/nutrition-card/pipeline-integration.md"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/NutritionCard.tsx",
              "src/components/track/nutrition/useNutritionStore.ts",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-02",
              "W0-02"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. TDD: Write failing Convex test for the food logging mutation. Write Playwright E2E test for the full flow: type food → stage → Log Food → appears in log.",
                "3. Implement the wiring code to make tests pass.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "tdd_targets": "Convex test for food logging mutation",
                "e2e_targets": "e2e/nutrition-card.spec.ts — full food logging flow",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Log Food button calls useFoodParsing.handleLogFood with reconstructed rawInput",
              "Staging clears on successful log",
              "Success toast shown",
              "Food logging goes through addSyncedLog with type='food'",
              "Convex test passes",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/NutritionCard.tsx (modified)",
              "src/components/track/nutrition/useNutritionStore.ts (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W3-02",
            "title": "Wire WaterModal to fluid logging pipeline",
            "description": "NutritionCard accepts onLogWater prop. Track.tsx passes handleLogFluid('Water', amountMl). Wire water progress data from useNutritionData + useNutritionGoals.\n\nPlaywright E2E: log water → verify progress bar updates → verify water count in calorie detail view.",
            "files_to_read": [
              "src/hooks/useHabitLog.ts",
              "src/pages/Track.tsx"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/NutritionCard.tsx",
              "src/components/track/nutrition/WaterModal.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-04",
              "W1-03"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Write Playwright E2E test: open water modal → log water → progress bar updates.",
                "3. Implement the wiring to make the test pass.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — water logging: modal → log → progress update",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "NutritionCard accepts onLogWater prop",
              "WaterModal calls handleLogFluid('Water', amountMl)",
              "Water progress data wired from useNutritionData + useNutritionGoals",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/NutritionCard.tsx (modified)",
              "src/components/track/nutrition/WaterModal.tsx (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W3-03",
            "title": "Wire StagingModal match status to FoodMatchingModal",
            "description": "Orange alert on unresolved staging items opens a lightweight in-staging registry search (not full FoodMatchingModal — staging items have no logId yet). Use searchFoodDocuments from shared/foodMatching.ts. When matched, update staging item's canonicalName and matchStatus.\n\nPlaywright E2E: stage an unresolved food → tap orange alert → search → select match → verify green tick.",
            "files_to_read": [
              "src/components/track/FoodMatchingModal.tsx",
              "shared/foodMatching.ts"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/StagingModal.tsx",
              "src/components/track/nutrition/useNutritionStore.ts",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-03",
              "W0-02"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Write Playwright E2E test: stage unresolved food → tap alert → search → match → green tick.",
                "3. Implement the wiring to make the test pass.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — match resolution: unresolved → search → match → green tick",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Orange alert on unresolved items is tappable",
              "Opens lightweight in-staging registry search",
              "When matched, staging item updates canonicalName and shows green tick",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/StagingModal.tsx (modified)",
              "src/components/track/nutrition/useNutritionStore.ts (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W3-04",
            "title": "Add error boundary around NutritionCard",
            "description": "NEW TASK — not in v1 plan. Create a NutritionCardErrorBoundary that wraps NutritionCard in Track.tsx. If NutritionCard crashes, show a fallback UI with 'Nutrition card failed to load' message and a retry button. This prevents the entire Track page from dying if NutritionCard has a runtime error.\n\nThe error boundary should log the error to console for debugging.\n\nThis task MUST be completed before W3-05 (mounting in Track page).",
            "files_to_read": [
              "src/pages/Track.tsx"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/NutritionCardErrorBoundary.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-02"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Build the NutritionCardErrorBoundary component.",
                "3. Write Playwright E2E test that verifies the error boundary catches errors and shows fallback UI (may need a test-only prop to trigger error).",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — error boundary catches crash and shows fallback",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Error boundary wraps NutritionCard",
              "Fallback UI shows 'Nutrition card failed to load' + retry button",
              "Error is logged to console",
              "Retry button re-mounts the component",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/NutritionCardErrorBoundary.tsx (new)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W3-05",
            "title": "Mount NutritionCard in Track page (with error boundary)",
            "description": "Replace FoodSection + FluidSection with NutritionCard (wrapped in NutritionCardErrorBoundary) in Track.tsx. Keep FoodMatchingModal, useFoodLlmMatching, useUnresolvedFoodQueue exactly as-is. Keep old sections alive but not rendered (rollback safety). Keep QuickCapture fluid handling for non-water habits.\n\nPlaywright E2E: Navigate to Track page → verify NutritionCard renders → verify old FoodSection/FluidSection are NOT visible.",
            "files_to_read": [
              "src/pages/Track.tsx",
              "src/components/track/panels/index.ts"
            ],
            "files_to_modify": [
              "src/pages/Track.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W3-01",
              "W3-02",
              "W3-04"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Write Playwright E2E test: Track page → NutritionCard visible → old sections not visible.",
                "3. Implement the mounting + error boundary wrapping.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — Track page renders NutritionCard, not old sections",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "NutritionCard renders on Track page wrapped in error boundary",
              "FoodSection and FluidSection no longer rendered (but files kept)",
              "FoodMatchingModal, useFoodLlmMatching, useUnresolvedFoodQueue unchanged",
              "QuickCapture still has fluid handling",
              "Playwright E2E test passes",
              "bun run typecheck + build pass",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/pages/Track.tsx (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W3-06",
            "title": "Wire inline feedback badge and live search",
            "description": "Staging count badge on Log Food button ('Log Food (3)'). Fuse.js index against real FOOD_REGISTRY. Search results show: canonical name, zone badge, portion info, '+' button to stage.\n\nPlaywright E2E: type 3+ chars → results appear → click + → staging count updates on Log Food button.",
            "files_to_read": [
              "shared/foodMatching.ts",
              "shared/foodRegistry.ts"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/useNutritionStore.ts",
              "src/components/track/nutrition/NutritionCard.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W2-02",
              "W1-01"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Write Playwright E2E test: type 3+ chars → results → + button → badge updates.",
                "3. Implement the feature to make the test pass.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — live search: type → results → stage → badge",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Staging count badge on Log Food button",
              "Fuse.js index against real FOOD_REGISTRY",
              "Search results show canonical name, zone badge, portion info, + button",
              "Results update on every keystroke after 3 chars",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/useNutritionStore.ts (modified)",
              "src/components/track/nutrition/NutritionCard.tsx (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W3-07",
            "title": "End-to-end integration test: full nutrition flow",
            "description": "NEW TASK — not in v1 plan. Write a comprehensive Playwright E2E test that verifies the FULL flow works end-to-end:\n\n1. Navigate to Track page\n2. Type a food in the search input (e.g., 'white rice')\n3. Add to staging via + button\n4. Adjust portion in staging modal\n5. Click Log Food\n6. Verify food appears in TodayLog\n7. Verify calorie ring updates\n8. Open calorie detail view\n9. Verify food appears under correct meal slot\n10. Delete the food\n11. Verify calorie ring decreases\n12. Log water via water modal\n13. Verify water progress bar updates\n\nThis is the single most important test in the entire plan. If this passes, the feature works.",
            "files_to_read": [],
            "files_to_modify": [
              "e2e/nutrition-full-flow.spec.ts"
            ],
            "depends_on": [
              "W3-05",
              "W3-06"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description and the entire e2e/nutrition-card.spec.ts file to understand existing test patterns.",
                "2. Write the comprehensive E2E test covering all 13 steps in the description.",
                "3. Run: bunx playwright test e2e/nutrition-full-flow.spec.ts",
                "4. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "5. If ANY test fails: document the exact failure, identify which Wave 3 task produced the bug, flag it.",
                "6. Update docs/WIP.md with task completion status.",
                "7. Commit to adams-rib branch with descriptive message referencing the task ID."
              ],
              "rollback_strategy": "This task only adds a test file — no rollback needed. If tests fail, they reveal bugs in earlier tasks that need fixing.",
              "file_conflict_rule": "No conflicts — this creates a new test file.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-full-flow.spec.ts — the critical integration test",
                "forbidden": "NO React component tests. NO mocks."
              }
            },
            "acceptance_criteria": [
              "E2E test covers all 13 steps in the description",
              "Test passes end-to-end",
              "Test runs in under 60 seconds",
              "bunx playwright test e2e/nutrition-full-flow.spec.ts passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bunx playwright test e2e/nutrition-full-flow.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "e2e/nutrition-full-flow.spec.ts (new)"
            ]
          }
        ]
      },
      {
        "wave": 4,
        "name": "Migration",
        "description": "Migrate non-water beverages from fluid to food. Clean up old sections.",
        "tasks": [
          {
            "id": "W4-01",
            "title": "Migrate non-water drinks to food suggestions",
            "description": "In NutritionCard search zero-state, add 'Common drinks' section from food registry (subcategory: hot_drink, juice, fizzy_drink). Tapping stages with correct portion. Remove FluidSection non-water presets from Track.tsx. Old fluid logs remain valid — no database migration.\n\nPlaywright E2E: open search → see drink suggestions in zero-state → tap one → stages correctly.",
            "files_to_read": [
              "src/components/track/panels/FluidSection.tsx",
              "shared/foodRegistryData.ts",
              "docs/nutrition-card/fluid-migration.md"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/NutritionCard.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W3-05",
              "W0-03"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Write Playwright E2E test: open search → drink suggestions visible → tap → stages correctly.",
                "3. Implement the feature to make the test pass.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — drink suggestions: zero-state → tap → stage",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Common drinks section in search zero-state",
              "Tapping drink suggestion stages with correct portion",
              "FluidSection non-water presets removed from Track rendering",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/NutritionCard.tsx (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W4-02",
            "title": "Remove old FoodSection and FluidSection from Track",
            "description": "Remove imports and rendering of FoodSection + FluidSection. Keep .tsx files alive (rollback safety). Update panels/index.ts. Ensure QuickCapture still has handleLogFluid for fluid habits. Run typecheck + lint.\n\nPlaywright E2E: Full regression — run ALL existing nutrition E2E tests to ensure nothing broke.",
            "files_to_read": [
              "src/pages/Track.tsx",
              "src/components/track/panels/index.ts"
            ],
            "files_to_modify": [
              "src/pages/Track.tsx",
              "src/components/track/panels/index.ts",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W4-01"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Remove FoodSection + FluidSection imports and rendering.",
                "3. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "4. Run FULL Playwright suite: bunx playwright test (not just nutrition tests — full regression)",
                "5. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "6. Check acceptance criteria — every single one must pass.",
                "7. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "8. Update docs/WIP.md with task completion status.",
                "9. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "10. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "bunx playwright test — FULL regression, not just nutrition tests",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "FoodSection and FluidSection imports removed from Track.tsx",
              "panels/index.ts updated",
              "QuickCapture still has handleLogFluid",
              "ALL Playwright E2E tests pass (full regression)",
              "bun run typecheck + lint pass",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run build",
              "bun run lint:fix",
              "bun run test",
              "bunx playwright test",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/pages/Track.tsx (modified)",
              "src/components/track/panels/index.ts (modified)"
            ]
          },
          {
            "id": "W4-03",
            "title": "Update TodayLog for nutrition card data",
            "description": "Ensure TodayLog correctly handles: food logs with mealSlot field, water logs (still type='fluid'), non-water drinks now as type='food'. Show mealSlot badge, portion + calorie info from registry.\n\nPlaywright E2E: Log food → verify TodayLog shows mealSlot badge + calorie info. Log water → verify TodayLog shows water entry correctly.",
            "files_to_read": [
              "src/components/track/today-log/groups/FoodGroupRow.tsx",
              "src/components/track/today-log/editors/FoodSubRow.tsx"
            ],
            "files_to_modify": [
              "src/components/track/today-log/groups/FoodGroupRow.tsx",
              "src/components/track/today-log/editors/FoodSubRow.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W3-05"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Write Playwright E2E test: log food → TodayLog shows mealSlot badge + calories. Log water → TodayLog shows water.",
                "3. Implement the changes to make tests pass.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — TodayLog: food with mealSlot badge, water entry",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "TodayLog shows mealSlot badge on food logs",
              "Water logs (type=fluid) display correctly",
              "Non-water drinks now as type=food display correctly",
              "Playwright E2E test passes",
              "bun run typecheck passes",
              "bun run test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/today-log/groups/FoodGroupRow.tsx (modified)",
              "src/components/track/today-log/editors/FoodSubRow.tsx (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          }
        ]
      },
      {
        "wave": 5,
        "name": "Polish",
        "description": "Dark mode, accessibility, edge cases, and full regression. Tests for earlier waves are already done — this wave focuses on visual polish, a11y hardening, and edge cases that weren't covered.",
        "tasks": [
          {
            "id": "W5-01",
            "title": "Dark mode and CSS variable audit",
            "description": "Audit all Nutrition card components for correct dark/light mode. All colors must use CSS variables. Test glass-card system, ring SVGs, modal backdrops, hover states, zone badges in both themes.\n\nPlaywright E2E: Toggle dark/light mode → verify no hardcoded colors break → verify ring SVGs render in both modes.",
            "files_to_read": [
              "src/index.css",
              "src/components/track/nutrition/*.tsx"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/NutritionCard.tsx",
              "src/components/track/nutrition/StagingModal.tsx",
              "src/components/track/nutrition/WaterModal.tsx",
              "src/components/track/nutrition/CalorieDetailView.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W3-05"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Audit all components for hardcoded hex values — replace with CSS variables.",
                "3. Write Playwright E2E test: toggle dark/light mode → verify components render correctly in both.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005 — test BOTH dark and light mode."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — dark/light mode toggle verification",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "All colors use CSS variables (no hardcoded hex except constants like cyan #38bdf8)",
              "glass-card system works in both themes",
              "Ring SVGs render in both modes",
              "Modal backdrops work in light mode",
              "Playwright E2E test passes for both themes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/*.tsx (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W5-02",
            "title": "Accessibility hardening",
            "description": "Apply D's patterns: aria-modal on all modals, role=dialog, focus management (move focus on open, return on close), focus trap, escape closes, screen reader labels on rings, search results as listbox/option.\n\nPlaywright E2E: Tab through all components → verify focus order → verify escape closes modals → verify aria attributes present.",
            "files_to_read": [
              "src/components/CLAUDE.md"
            ],
            "files_to_modify": [
              "src/components/track/nutrition/NutritionCard.tsx",
              "src/components/track/nutrition/StagingModal.tsx",
              "src/components/track/nutrition/WaterModal.tsx",
              "src/components/track/nutrition/CalorieDetailView.tsx",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W3-05"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description, acceptance criteria, and files_to_read thoroughly before writing any code.",
                "2. Add aria attributes, focus management, focus traps to all modal components.",
                "3. Write Playwright E2E test: tab navigation → focus order → escape closes → aria attributes present.",
                "4. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "5. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "6. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "7. Check acceptance criteria — every single one must pass.",
                "8. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "9. Update docs/WIP.md with task completion status.",
                "10. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "11. Browser-test via Claude-in-Chrome on localhost:3005 — test keyboard navigation."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "e2e_targets": "e2e/nutrition-card.spec.ts — a11y: tab navigation, focus traps, aria attributes",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "aria-modal on all modals",
              "role=dialog on modal containers",
              "Focus management (move on open, return on close)",
              "Focus trap in modals",
              "Escape closes all modals",
              "Screen reader labels on rings and buttons",
              "Playwright E2E test passes",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/*.tsx (modified)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W5-03",
            "title": "Edge cases and error states",
            "description": "Handle: first-time user (zero-state), no API key (auto-matching unavailable), network error (don't clear staging), long food names (truncate), calorie/water goal of 0 (no division by zero), 20+ staging items (warning), special chars in search (sanitize), rapid +/- (no negatives), empty staging Log Food (disabled).\n\nTDD: Write Vitest tests for edge cases in the reducer (goal=0 division, rapid +/- bounds, special char sanitization). Playwright E2E: test zero-state, empty staging disabled button.",
            "files_to_read": [],
            "files_to_modify": [
              "src/components/track/nutrition/NutritionCard.tsx",
              "src/components/track/nutrition/StagingModal.tsx",
              "src/components/track/nutrition/CalorieDetailView.tsx",
              "src/components/track/nutrition/WaterModal.tsx",
              "src/components/track/nutrition/__tests__/useNutritionStore.test.ts",
              "e2e/nutrition-card.spec.ts"
            ],
            "depends_on": [
              "W3-05"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. READ the task description and acceptance criteria thoroughly.",
                "2. TDD: Write failing Vitest tests for edge cases in the reducer: goal=0, rapid +/- bounds, special char sanitization, 20+ staging warning. Watch them fail. Then fix.",
                "3. Write Playwright E2E tests: zero-state rendering, empty staging disables Log Food.",
                "4. Implement edge case handling in components.",
                "5. Run ALL verification commands in order: bun run typecheck → bun run build → bun run lint:fix → bun run test → bun run format",
                "6. Run Playwright: bunx playwright test e2e/nutrition-card.spec.ts",
                "7. SMOKE TEST: Verify dev server is still running — curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "8. Check acceptance criteria — every single one must pass.",
                "9. If ANY verification or acceptance criteria fails: git stash your changes, report what failed, do NOT commit broken code.",
                "10. Update docs/WIP.md with task completion status.",
                "11. Commit to adams-rib branch with descriptive message referencing the task ID.",
                "12. Browser-test via Claude-in-Chrome on localhost:3005 — test each edge case."
              ],
              "rollback_strategy": "If verification fails after 2 attempts to fix: git stash, report the failure with exact error output, and flag the task as BLOCKED in docs/WIP.md.",
              "file_conflict_rule": "If another task in your wave modifies the same file, those tasks MUST run sequentially, not in parallel.",
              "testing_rules": {
                "tdd_targets": "useNutritionStore.test.ts — edge cases: goal=0, rapid +/-, special chars, 20+ staging",
                "e2e_targets": "e2e/nutrition-card.spec.ts — zero-state, empty staging disabled",
                "forbidden": "NO React component tests."
              }
            },
            "acceptance_criteria": [
              "Zero-state renders for first-time user",
              "No division by zero with goal=0",
              "Long food names truncated",
              "Empty staging disables Log Food button",
              "Network error doesn't clear staging",
              "Rapid +/- doesn't go negative",
              "Edge case Vitest tests pass",
              "Playwright E2E tests pass",
              "Dev server still returns 200 on localhost:3005"
            ],
            "verification": [
              "bun run typecheck",
              "bun run test",
              "bun run build",
              "bunx playwright test e2e/nutrition-card.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "src/components/track/nutrition/*.tsx (modified)",
              "src/components/track/nutrition/__tests__/useNutritionStore.test.ts (extended)",
              "e2e/nutrition-card.spec.ts (extended)"
            ]
          },
          {
            "id": "W5-04",
            "title": "Full regression: all tests pass, all E2E pass",
            "description": "Final verification task. Run the ENTIRE test suite (Vitest + Playwright) and fix any failures. This is the gate before merge.\n\nThis task also runs the nutrition-full-flow.spec.ts integration test one final time to confirm end-to-end works after all polish changes.",
            "files_to_read": [],
            "files_to_modify": [],
            "depends_on": [
              "W5-01",
              "W5-02",
              "W5-03"
            ],
            "completed": false,
            "task_protocol": {
              "steps": [
                "1. Run bun run test — ALL unit/Convex tests must pass.",
                "2. Run bunx playwright test — ALL E2E tests must pass.",
                "3. Run bunx playwright test e2e/nutrition-full-flow.spec.ts — the critical integration test must pass.",
                "4. Run bun run typecheck && bun run build && bun run lint:fix — zero errors.",
                "5. SMOKE TEST: curl -s -o /dev/null -w '%{http_code}' http://localhost:3005 must return 200.",
                "6. If ANY failure: fix it, re-run everything, repeat until green.",
                "7. Update docs/WIP.md with final completion status.",
                "8. Update docs/WORK-QUEUE.md marking the entire nutrition card feature as COMPLETE.",
                "9. Commit to adams-rib branch: 'W5-04: Full regression — all tests green, feature complete'."
              ],
              "rollback_strategy": "If a failure cannot be fixed: document it in docs/WIP.md as a known issue with severity and workaround. Do not mark the feature as complete.",
              "file_conflict_rule": "No conflicts — this is a verification-only task.",
              "testing_rules": {
                "scope": "ALL tests — bun run test + bunx playwright test",
                "forbidden": "Do NOT skip any failing tests. Do NOT mark tests as .skip."
              }
            },
            "acceptance_criteria": [
              "bun run test — ALL pass, zero failures",
              "bunx playwright test — ALL pass, zero failures",
              "bunx playwright test e2e/nutrition-full-flow.spec.ts — passes",
              "bun run typecheck — zero errors",
              "bun run build — succeeds",
              "bun run lint:fix — zero errors",
              "Dev server returns 200 on localhost:3005",
              "docs/WIP.md updated",
              "docs/WORK-QUEUE.md updated"
            ],
            "verification": [
              "bun run typecheck",
              "bun run build",
              "bun run lint:fix",
              "bun run test",
              "bunx playwright test",
              "bunx playwright test e2e/nutrition-full-flow.spec.ts",
              "curl -s -o /dev/null -w '%{http_code}' http://localhost:3005"
            ],
            "outputs": [
              "docs/WIP.md (updated)",
              "docs/WORK-QUEUE.md (updated)"
            ]
          }
        ]
      }
    ]
  }
}